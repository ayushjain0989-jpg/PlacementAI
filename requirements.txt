# Pinned deliberately. These versions are the ones models/*.pkl was created with, and
# joblib pickles are not guaranteed to load across scikit-learn versions -- an unpinned
# file previously produced an InconsistentVersionWarning (pickled 1.9.0, loaded 1.8.0)
# on the deployment host. Bump these together and retrain.

Flask==3.1.3
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
catboost==1.2.10
xgboost==3.4.1
shap==0.52.0
joblib==1.6.0
gunicorn==26.2.0
